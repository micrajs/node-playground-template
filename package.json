{
  "name": "@micra/node-playground-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "NODE_ENV=production node .config/build",
    "build:dev": "NODE_ENV=development node .config/build",
    "type:check": "tsc -p tsconfig.json",
    "test": "jest --config jest.config.js",
    "start": "yarn nodemon --config .config/dev/nodemon.json"
  },
  "devDependencies": {
    "@micra/cli": "^0.0.25",
    "@types/jest": "^26.0.22",
    "dotenv-flow": "^3.2.0",
    "esbuild": "^0.11.4",
    "esbuild-jest": "^0.5.0",
    "jest": "^26.6.3",
    "jest-docblock": "^26.0.0",
    "jest-runner": "^26.6.3",
    "jest-runner-groups": "^2.0.1",
    "jest-transform-stub": "^2.0.0",
    "msw": "^0.28.1",
    "node-factory": "^0.4.1",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  },
  "dependencies": {
  }
}
